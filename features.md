## "_" is an ignored character for numbers
The character _"\_"_ is an ignored character in numbers, for example, `1_000_000` = `1000000`
This doesn't apply for names given throughout the code, for example, `x` != `x_` != `_x` != `_x_`